apiVersion: v1
kind: Service
metadata:
  name: {{ include "docker-workshop.fullname" . }}
  labels:
    {{- include "docker-workshop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docker-workshop.selectorLabels" . | nindent 4 }}
